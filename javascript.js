document.querySelector('.cta-button').addEventListener('click', () => {
    alert('Let’s plan your next adventure!');

    window.onload = function() {
        console.log("Welcome to our page!");
        // Your code to run after the page has finished loading
    };
    
});
